# Зависимости для CI (GitHub Actions): API + тесты, без torch/transformers
-r requirements-api.txt

pytest==8.3.4
httpx>=0.27.0

# Линтеры (проверяем tests/; app/ — позже)
flake8==7.1.1
black==24.10.0
